import 'package:flutter/material.dart';

class ResetFunc extends StatelessWidget {
  const ResetFunc({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Container(
      child: Text('shit'),
    );
  }
}
